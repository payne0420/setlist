# core dependencies
mutagen>=1.46.0
PyQt5>=5.15.9
requests>=2.32.2
yt-dlp>=2024.8.6

# lossless (Real FLAC) backend: RFC-6238 TOTP for the Spotify ISRC flow +
# AES-GCM for the Qobuz MusicDL / Amazon proxy debug keys
pyotp>=2.9.0
cryptography>=42.0.0
# Opt-in librespot backend (Spotify native ~320k OGG). Alpha library, PINNED to a
# known-good commit and contained behind backends/librespot/_librespot/ so an upstream
# break degrades to YouTube rather than bricking Setlist. Pure Python — no Rust
# toolchain, no extra native binary; ffmpeg is NOT needed for the OGG path.
librespot @ git+https://github.com/kokarare1212/librespot-python.git@18104622b3be02062f1f8abe8dafc396413e9784
# librespot's own deps (auto-installed; listed for reference + PyInstaller hiddenimports):
#   defusedxml protobuf==3.20.1 pycryptodomex pyogg requests websocket-client zeroconf

# transitive (auto-installed but listed for reference)
certifi>=2023.7.22
charset-normalizer>=3.2.0
idna>=3.4
urllib3>=2.0.4
